*** Probes ***

+ FPing

binary = /usr/bin/fping

+ EchoPingHttp

binary = /usr/bin/echoping

+ EchoPingHttps

binary = /usr/bin/echoping

+ speedtest
binary = /usr/local/bin/speedtest-cli
timeout = 300
step = 3600
offset = random
pings = 3

++ speedtest-download
measurement = download

++ speedtest-upload
measurement = upload

